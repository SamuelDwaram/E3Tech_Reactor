<UserControl x:Class="E3.ReactorManager.LiveGraphManager.Views.LiveGraphViewsContainerManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:E3.ReactorManager.LiveGraphManager.Views"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" CommandParameter="{Binding DataContext.CurrentFieldDeviceName,RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=Grid}}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <local:LiveGraphView DataContext="{Binding LiveGraphViewDataContext}"/>
    </Grid>
</UserControl>
